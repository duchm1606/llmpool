Task 1: OAuth Config Contract - Validation Tests
Status: COMPLETED

Test Results: All validation tests passed

1. TestLoad_RequiresOAuthCodexTimeout
   - Validates timeout must be > 0
   - Test: Setting LLMPOOL_OAUTH_CODEX_TIMEOUT to "0" correctly triggers error
   - Outcome: PASS

2. TestLoad_RequiresOAuthCodexSessionTTL
   - Validates session_ttl must be > 0
   - Test: Setting LLMPOOL_OAUTH_CODEX_SESSION_TTL to "0" correctly triggers error
   - Outcome: PASS

3. TestLoad_OAuthCodexUsesDefaults
   - Validates default OAuth config values are loaded when not overridden
   - Tests default values:
     * auth_url: https://auth.openai.com/authorize
     * token_url: https://auth.openai.com/token
     * device_url: https://auth.openai.com/device/code
     * poll_url: https://auth.openai.com/device/poll
   - Outcome: PASS

Summary:
- All 6 config tests pass
- Config loading respects: defaults → file → env override precedence
- Required OAuth config fields validate correctly
- Timeout and TTL duration fields validate properly
